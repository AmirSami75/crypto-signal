---
# Self-hosted production (app + local Postgres container).
# Replace the host, user, and key path before first use.
all:
  hosts:
    prod-01:
      ansible_host: 192.0.2.10
      ansible_user: deploy
      ansible_ssh_private_key_file: ~/.ssh/crypto-signal-prod.pem
